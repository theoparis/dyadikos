[package]
name = "dyadikos-math"
version = "0.0.1"
edition = "2021"
license = "MIT"

[dependencies]
glam = "0.21.2"
serde = { version = "1.0.140", features = ["derive"], optional = true }
bytemuck = { version = "1.11.0", features = ["derive"] }

[features]
serialize = ["serde"]
